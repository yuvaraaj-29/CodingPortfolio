class Solution {
    public void reverseString(char[] s) {
        int i;
        char[] b = new char[s.length];
        int k=0;
        for(i=s.length-1;i>=0;i--)
        {
            b[k++] = s[i];
        }
        for(i=0;i<s.length;i++)
        {
            s[i] = b[i];
        }
    }
}

class Solution {
    public int balancedStringSplit(String s) {
    int r=0,i;
    int c1=1;
    for(i=0;i<s.length()-1;i++)
    {
        char c = s.charAt(i);
        if(c == 'R') r++;
        else r--;
        if(r==0) c1++;
    }
    return c1;
    }
}
